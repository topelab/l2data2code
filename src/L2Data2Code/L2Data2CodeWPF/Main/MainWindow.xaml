<mah:MetroWindow x:Class="L2Data2CodeWPF.Main.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
                 xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                 xmlns:vm="clr-namespace:L2Data2CodeWPF.Main"
                 xmlns:cb="clr-namespace:L2Data2CodeWPF.Controls.CommandBar"
                 xmlns:tp="clr-namespace:L2Data2CodeWPF.Controls.TablePanel"
                 xmlns:mp="clr-namespace:L2Data2CodeWPF.Controls.MessagePanel"
                 xmlns:s="clr-namespace:L2Data2CodeWPF.SharedLib"
                 xmlns:str="clr-namespace:L2Data2CodeUI.Shared.Localize;assembly=L2Data2CodeUI.Shared"
                 mc:Ignorable="d"
                 d:DataContext="{d:DesignInstance vm:MainWindowVM}"
                 Title="{x:Static str:Strings.Title}"
                 Height="600"
                 Width="850"
                 WindowStartupLocation="CenterScreen"
                 FontSize="14"
                 GlowBrush="{DynamicResource AccentColorBrush}">
    
    <Grid x:Name="PrincipalGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40*" />
            <ColumnDefinition Width="30*" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="0" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <cb:CommandBarUserControl Grid.Row="1"
                                  Grid.ColumnSpan="3" />

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="200*" />
            </Grid.RowDefinitions>
            <Grid Margin="10,10,10,10"
                  IsEnabled="{Binding Working, Converter={x:Static s:Converters.EnableIfFalse}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="200*" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="163*" />
                    <ColumnDefinition Width="344*" />
                </Grid.ColumnDefinitions>

                <TextBlock TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Text="{x:Static str:Strings.Template}"
                           HorizontalAlignment="Right"
                           Margin="10,0" />
                <ComboBox Grid.Column="1"
                          x:Name="cbTemplate"
                          ItemsSource="{Binding TemplateList}"
                          SelectedItem="{Binding SelectedTemplate}"
                          Margin="0,5" />

                <TextBlock Grid.Row="2"
                           HorizontalAlignment="Right"
                           Margin="10,0"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Text="{x:Static str:Strings.Vars}"
                           Visibility="{Binding VarsVisible, Converter={x:Static s:Converters.VisibleIfTrue}}" />
                <Grid Grid.Row="2"
                      Visibility="{Binding VarsVisible, Converter={x:Static s:Converters.VisibleIfTrue}}"
                      Grid.Column="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ComboBox Grid.Column="0"
                              x:Name="cbVars"
                              ItemsSource="{Binding VarsList}"
                              SelectedItem="{Binding SelectedVars}"
                              Margin="0,5" />

                </Grid>
                <TextBlock Grid.Row="1"
                           HorizontalAlignment="Right"
                           Margin="10,0"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Text="{x:Static str:Strings.DataSource}" />
                <ComboBox Grid.Row="1"
                          Grid.Column="1"
                          x:Name="cbArea"
                          ItemsSource="{Binding DataSourceList}"
                          SelectedItem="{Binding SelectedDataSource}"
                          Margin="0,5" />

                <TextBlock Grid.Row="3"
                           HorizontalAlignment="Right"
                           Margin="10,0"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           Text="{x:Static str:Strings.Module}" />
                <ComboBox Grid.Row="3"
                          Grid.Column="1"
                          x:Name="cbModule"
                          ItemsSource="{Binding ModuleList}"
                          SelectedItem="{Binding SelectedModule}"
                          Margin="0,5" />

                <TextBlock Grid.Row="4"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Margin="10,0"
                           Text="{x:Static str:Strings.SaveAt}" />
                <TextBox Grid.Row="4"
                         Grid.Column="1"
                         Margin="0,5"
                         Text="{Binding OutputPath}"
                         IsReadOnly="True" />

                <mah:ToggleSwitch Grid.Row="5"
                                  Grid.Column="1"
                                  Margin="-50,0"
                                  IsOn="{Binding SetRelatedTables}"
                                  Content="{x:Static str:Strings.SelectRelatedTables}"
                                  TextBlock.FontSize="12" />

                <mah:ToggleSwitch Grid.Row="6"
                                  Grid.Column="1"
                                  Margin="-50,0"
                                  IsOn="{Binding EmptyFolders}"
                                  IsEnabled="{Binding GenerateOnlyJson, Converter={x:Static s:Converters.EnableIfFalse}}"
                                  Content="{x:Static str:Strings.EmptyFolders}"
                                  TextElement.FontSize="12" />

                <mah:ToggleSwitch Grid.Row="7"
                                  Grid.Column="1"
                                  Margin="-50,0"
                                  IsOn="{Binding GenerateOnlyJson}"
                                  Visibility="{Binding GenerateOnlyJsonVisible, Converter={x:Static s:Converters.VisibleIfTrue}}"
                                  Content="{x:Static str:Strings.GenerateOnlyJson}"
                                  TextElement.FontSize="12" />

                <Button Grid.Column="1"
                        Grid.Row="8"
                        Command="{Binding GenerateCodeCommand}"
                        CommandParameter="{Binding}"
                        Height="72"
                        Margin="0, 10"
                        Content="{iconPacks:Material Kind=RunFast, Height=64, Width=64}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Top"
                        ToolTip="{x:Static str:Strings.Generate}"
                        Foreground="{DynamicResource MahApps.Brushes.Accent}">
                </Button>
            </Grid>

            <mp:MessagePanelUserControl Grid.Row="1" />

        </Grid>

        <tp:TablePanelUserControl Grid.Row="2"
                                  Grid.Column="1" />

    </Grid>
</mah:MetroWindow>
